# 全量设计覆盖矩阵

本文件以《工作时间与工作智能管理平台_全量产品与交互设计报告》为验收基线。它只记录已经能够从数据库、服务端接口、前端交互和测试中验证的能力；数据库里存在一张表、前端出现一个入口，均不单独算“完成”。

状态定义：

- **已实现**：主链路有真实持久化、服务端权限/校验和对应界面。
- **部分实现**：核心链路可用，但设计报告要求的分支、交互或验收项仍缺失。
- **未实现**：当前没有可交付的完整链路；不得在产品说明中宣称已完成。
- **外部配置**：代码链路已实现，但生产环境需要第三方凭据或基础设施后才可用。

## 当前结论

系统已经是可部署的真实业务应用，不是静态 Demo；但它还没有达到设计报告所定义的“全量完成”。当前最稳固的是账号与组织基础、工时事实、审批、项目版本操作、薪资核心计算、实时事件、权限感知搜索和附件安全传输骨架。当前主要缺口集中在分析图表高级联动、AI 页面级上下文与 Provider 健康历史、浏览器 Push、后台 XLSX/PDF 导出、完整离线逐字段合并以及运维恢复演练。

| 领域 | 状态 | 已验证能力 | 尚缺的设计验收项 |
| --- | --- | --- | --- |
| 唯一 Owner 与账号 | 已实现 | 数据库唯一 Owner、邮箱/手机号登录标识、密码、会话、TOTP、停用、所有权转移 | 自动邮件/SMS 需外部 Provider |
| 白名单邀请与找回 | 已实现 / 外部配置 | 邮箱或手机号至少一个、两者可同时填、一次性手工邀请链接、首次设密、Owner 手工重置链接、令牌单次使用与审计 | 自动投递需 SMTP/Twilio；未配置时明确降级为手工链接 |
| 角色 / 岗位 / 专业身份 | 已实现 | 三层独立模型、RBAC + Scope、身份申请与审核、服务端裁剪 | 更大规模组织下的虚拟列表与性能基准仍需压测 |
| 工时记录与计时器 | 已实现 | 计时开始/暂停/休息/继续/结束、手工事实与计划、跨午夜、休息扣除、重叠校验、7 天补录、版本与状态机、结算后更正 | 本地草稿与服务端草稿发生并发冲突时的逐字段差异合并页仍缺失 |
| 多端同步 | 已实现 | PostgreSQL 事实源、SSE 事件流、断线重连、游标续传、客户端缓存失效、计时器服务端恢复 | 多 Web 实例 Pub/Sub 和数百并发专项压测未完成 |
| 附件 / 证据 | 已实现 / 外部配置 | 任意格式、多选队列、数量无应用层上限、私有预签名直传、SHA-256/大小核验、短时私有下载、版本替换、失败重试、软删除与审计、URL/文字证据 | Render 当前必须配置外部 S3/R2；病毒扫描、对象生命周期回收与恢复演练未完成 |
| 项目树 / 分支 | 部分实现 | 画布、列表、时间轴、节点/边、分支、版本、Diff、回滚、归档/恢复、回收站、进度模式和成员关联 | 分支 merge 的完整冲突解决、超大图性能验收和移动端手势回归仍不完整 |
| 组织树 | 部分实现 | 树/列表、层级节点、成员、负责人、检索和分层身份展示 | 拖拽调整组织层级的完整历史体验和超大组织画布验收仍不完整 |
| 日历 | 部分实现 | 日/周/月及列表式记录入口、真实工时块、草稿改期、重叠校验、里程碑叠加 | 所有视图的拖拽 Resize、跨端手势和管理 Scope 日历专项测试仍未全部覆盖 |
| 审批 | 已实现 | 提交、待审、批量通过、退回原因、异常标记、版本快照、权限范围 | 复杂多级审批规则和详情侧栏中的完整工资影响预览仍需扩展 |
| 薪资 | 部分实现 | 六种方案数据模型、版本、生效区间、Decimal 计算、周期、预估、结算快照、工资单与调整项；Owner 配置、成员只读自身 | 节假日日历、项目制/混合制的全部 UI 规则编辑器、规则历史对比与完整异常用例仍需补齐 |
| 数据分析 | 部分实现 | 服务端权限聚合、KPI、时间趋势、项目分布、工作类型、审批分布、24 小时节奏、Calendar heatmap、记录到计薪漏斗、真实明细、全屏、PNG 与 CSV/JSON 数据导出 | Sankey、工资瀑布、旭日、预测带、跨图联动刷选和管理端专项图尚未全量实现 |
| AI 工作智能 | 部分实现 | 唯一 Owner 才可配置 OpenAI-compatible Base URL/模型/API Key；Key 加密且不回显；成员只能使用；服务端先聚合并裁剪事实；今日/周/月、节奏、项目进展/阻塞、组织总结七类任务；异步队列、去重、额度、超时、取消、人工重试、来源记录 | 工资解释、记录级上下文、Provider 健康检查历史、页面级 Copilot 与 AI 修改建议确认链路尚未全量实现 |
| 智能提醒 | 部分实现 | 站内通知、类别偏好、勿扰字段、本次忽略/整类静音、长计时、重叠、连续工作、短休息、个人基线变化、项目临期、阻塞老化、审批退回、结算待审、身份和 AI 任务结果规则及去重 | 后台导出完成通知与 VAPID 浏览器 Push 订阅/失效清理尚未实现 |
| 导入 / 导出 | 部分实现 | CSV 导入预览、整批事务确认、内容哈希、权限审计；CSV/JSON 工时导出及字段脱敏 | XLSX/PDF、后台生成、进度、到期对象下载和更多业务对象尚未实现 |
| 全局搜索 | 已实现 | 服务端按 RBAC + Scope 裁剪工作记录、项目、节点、成员、附件和 AI 报告；命令面板防抖检索并跳转到实体上下文 | 更大数据量下可升级 PostgreSQL FTS/三元索引，但不影响当前数百人规模主链路 |
| PWA / 响应式 / 无障碍 | 部分实现 | PWA 构建、桌面/移动导航、侧栏收起与调宽、主要页面响应式、键盘焦点与 reduced-motion 基础 | 设计报告指定的全部 1440×900、1920×1080、390×844 视觉回归和完整无障碍审计仍需持续补齐 |
| 备份与灾难恢复 | 部分实现 | Render PostgreSQL、不可变业务快照、审计和部署健康检查 | 定期导出、R2 与数据库恢复演练、RPO/RTO 记录和自动告警仍未完成 |

## AI 配置与调用核对

- 配置读取和写入都会在服务端查询 `organization_owners`，不能依赖前端隐藏按钮；非唯一 Owner 返回 403。
- API Key 使用独立 `AI_CONFIG_ENCRYPTION_KEY` 加密，浏览器只看到“已配置/未配置”，不会收到密钥原文。
- Worker 使用 OpenAI-compatible `POST {baseUrl}/chat/completions`，发送 Bearer、`model`、`temperature: 0.2`、`max_tokens` 和严格 JSON 消息；输出在入库前再次做结构校验。
- 默认 `glm-4.7-flash` 是当前智谱官方接口列出的有效低成本模型。设计报告提到的 `glm-5.3-flash` 当前不在官方模型枚举中，因此不能为了字面一致而默认调用一个未确认存在的模型。
- “已配置”不等于“供应商实时健康”。真实可用性的最终验收必须由 Owner 生成一份报告并确认 Worker 完成；供应商错误会明确失败，绝不生成伪报告。

## 发布门槛

每次声称某一行升级为“已实现”前，至少要同时满足：数据库迁移可重复执行、API 服务端权限测试、核心状态机/金额单元测试、桌面和移动关键路径 E2E、空/错/加载状态、Render 构建与健康检查，以及无真实账号、密钥或虚拟业务数据进入 Git。
