# 全量设计覆盖矩阵

本文件以《工作时间与工作智能管理平台_全量产品与交互设计报告》为验收基线。它只记录已经能够从数据库、服务端接口、前端交互和测试中验证的能力；数据库里存在一张表、前端出现一个入口，均不单独算“完成”。

状态定义：

- **已实现**：主链路有真实持久化、服务端权限/校验和对应界面。
- **部分实现**：核心链路可用，但设计报告要求的分支、交互或验收项仍缺失。
- **未实现**：当前没有可交付的完整链路；不得在产品说明中宣称已完成。
- **外部配置**：代码链路已实现，但生产环境需要第三方凭据或基础设施后才可用。

## 当前结论

系统已经是可部署的真实业务应用，不是静态 Demo；但仍不能把尚无真实链路或专项验收的条目写成“全量完成”。当前最稳固的是账号与组织基础、工时事实、审批、可恢复项目版本操作、薪资核心计算、带滚动回测的工时/薪资预测、权限裁剪的交互式分析、实时事件、权限感知搜索、任意格式附件安全传输与被动格式预览、工时后台文件导出、个人工资事实解释、页面上下文 Copilot 和浏览器 Push 投递链。当前主要缺口集中在分析图表的完整跨图刷选与管理成本预测、AI 修改建议确认链、更多业务对象导出、完整离线逐字段合并以及运维恢复演练。

| 领域 | 状态 | 已验证能力 | 尚缺的设计验收项 |
| --- | --- | --- | --- |
| 唯一 Owner 与账号 | 已实现 | 数据库唯一 Owner、邮箱/手机号登录标识、密码、会话、TOTP、停用、所有权转移 | 自动邮件/SMS 需外部 Provider |
| 白名单邀请与找回 | 已实现 / 外部配置 | 邮箱或手机号至少一个、两者可同时填、一次性手工邀请链接、服务端有效性预检、首次设密、Owner 手工重置链接、令牌单次使用与审计；待加入邀请可撤销并释放联系方式，已激活成员仅逻辑移除并保留事实；成员页显示接受时间、最近活动、在线状态和活跃端数 | 自动投递需 SMTP/Twilio；未配置时明确降级为手工链接 |
| 角色 / 岗位 / 专业身份 | 已实现 | 三层独立模型、RBAC + Scope、身份申请与审核、服务端裁剪；普通成员只看同项目成员的公开工作摘要、专业身份与最后有效工作时间，不读取他人的完整区间、总时长、证据或薪资；Owner 可看组织范围完整事实 | 更大规模组织下的虚拟列表与性能基准仍需压测 |
| 工时记录与计时器 | 已实现 | 计时开始/暂停/休息/继续/结束、手工事实与计划、跨午夜、休息扣除、重叠校验、7 天补录、版本与状态机、结算后更正 | 本地草稿与服务端草稿发生并发冲突时的逐字段差异合并页仍缺失 |
| 多端同步 | 已实现 | PostgreSQL 单一事实源、事务后 Outbox、WebSocket 组织事件、断线退避重连、通用查询失效、计时器服务端恢复；登录、退出和同浏览器多标签身份变化会先清除上一账号的业务缓存，分析查询键包含成员身份且不复用旧结果 | 多 Web 实例 Pub/Sub 和数百并发专项压测未完成 |
| 附件 / 证据 | 已实现 / 外部配置 | 审批前强制至少一项审核可见证据；任意格式、多选队列、选择后本地详情；提交人、审核人和团队动态共享权限受控详情；图片、音视频、PDF、TXT/README/JSON/CSV 安全内嵌，活跃/压缩/Office/未知格式元数据与强制下载；数量无应用层上限、私有预签名直传、SHA-256/大小核验、短时访问、版本替换、失败重试、软删除与审计、URL/文字证据、实时跨端刷新 | Render 当前必须配置外部 S3/B2；病毒扫描、对象生命周期回收与恢复演练未完成 |
| 项目树 / 分支 | 部分实现 | 画布、列表、层级时间轴、节点/边、工作线、版本、Diff、回滚、归档/恢复、回收站、进度模式和成员关联；选中节点可直接新建同线子节点，也可派生带真实入口节点的并行工作线；合并时完整复制节点层级、内部关联和仍在项目内的负责人，并把来源根重新挂到最初来源节点；旧空分支会实体化为子节点，避免“合并成功但内容消失”；移动端提供视口级全屏画布、触控尺寸控制和列表兜底；节点关联工作执行本人/Owner 完整、同项目他人公开摘要的字段级裁剪 | 同一业务语义的人工三方冲突选择、超大图性能验收和复杂移动端手势回归仍不完整 |
| 组织树 | 部分实现 | 树/列表、层级节点、成员、负责人、检索和分层身份展示 | 拖拽调整组织层级的完整历史体验和超大组织画布验收仍不完整 |
| 日历 | 部分实现 | 日/周/月及列表式记录入口、真实工时块、草稿改期、重叠校验、里程碑叠加 | 所有视图的拖拽 Resize、跨端手势和管理 Scope 日历专项测试仍未全部覆盖 |
| 审批 | 已实现 | 提交、待审、批量通过、退回原因、异常标记、版本快照、权限范围 | 复杂多级审批规则和详情侧栏中的完整工资影响预览仍需扩展 |
| 薪资 | 部分实现 | 六种方案数据模型、成员级独立配置、版本、生效区间、Decimal 计算、周期、预估、结算快照、工资单与调整项；Owner 配置、成员只读自身；个人当前应结/已结累计/累计薪资、每日薪资、周期趋势、薪资构成瀑布和可追溯明细；未来可变薪资使用星期季节性、工作日/周末、指数平滑与 Theil-Sen 稳健趋势的自适应组合，以本人历史滚动回测误差确定权重和经验区间，已知未来记录与奖励单独精确计算 | 节假日日历、项目制/混合制的全部 UI 规则编辑器、规则历史对比与完整异常用例仍需补齐 |
| 数据分析 | 部分实现 | 服务端权限与时间范围裁剪聚合；项目/类型/成员/组织单元/审核/来源六类组合筛选；KPI、时间趋势、项目分布、工作类型、审批分布、24 小时节奏、Calendar heatmap、记录到计薪漏斗、项目→类型→审核 Sankey、项目/类型旭日、团队负荷、项目投入与加权进度、异常分布、项目与分类对账；图表点击回写筛选、全屏、PNG 与 CSV/JSON 数据导出；未来预测保留零工时日，以四类候选模型和最多 28 个历史日滚动回测自动加权，展示 WAPE、历史区间覆盖率、90% 经验残差与模型分歧区间，少于七个连续自然日不会伪造结论 | 任意框选后的全图同步高亮、计划/实际偏差与管理成本预测、超大数据量降采样专项验收仍需补齐 |
| AI 工作智能 | 部分实现 | 唯一 Owner 才可配置 OpenAI-compatible Base URL/模型/API Key；Key 加密且不回显；成员只能使用；服务端按权限裁剪聚合、成员、项目、节点与记录级事实；今日/周/月、节奏、项目进展/阻塞、组织总结和连续对话；右上角页面 Copilot 在桌面/移动端直接使用当前页面与授权实体作为焦点，并可带同一会话进入洞察中心；本人薪资解释与聊天中的本人薪资上下文只读取最新工资事实、分项、每日金额和结算状态，禁止团队工资装配、重算与税费猜测；异步队列、去重、额度、超时、取消、人工重试、可见来源记录；Owner 二次鉴权的低成本连接测试、30 秒防刷、脱敏故障与历史延迟 | AI 修改建议确认链路尚未全量实现 |
| 智能提醒 | 部分实现 / 外部配置 | 站内通知、分类与临时静音；VAPID 浏览器授权、加密多设备订阅、按分类推送、跨午夜免打扰、幂等投递、失败退避、404/410 停用、30 天清理和退出解绑；长计时、重叠、连续工作、短休息、个人基线变化、项目临期、阻塞老化、审批退回、结算待审、身份、AI 与后台导出完成/失败规则及去重 | 生产 Push 需配置 VAPID；通知邮件通道尚未实现 |
| 导入 / 导出 | 部分实现 / 外部配置 | CSV 导入预览、整批事务确认、内容哈希、权限审计；工时 CSV/JSON/XLSX/PDF 后台生成、权限与字段快照、进度、取消、失败重试、私有对象存储、二次鉴权短时下载、SHA-256、24 小时过期清理与通知；同步 CSV/JSON 兼容接口仍保留 | 生产需配置 S3/R2；项目、薪资、审批、证据清单等更多业务对象尚未加入同一后台框架 |
| 全局搜索 | 已实现 | 服务端按 RBAC + Scope 裁剪工作记录、项目、节点、成员、附件和 AI 报告；命令面板防抖检索并跳转到实体上下文 | 更大数据量下可升级 PostgreSQL FTS/三元索引，但不影响当前数百人规模主链路 |
| PWA / 响应式 / 无障碍 | 部分实现 | 可安装 manifest、独立显示模式、工作/项目/分析快捷入口、离线页面壳、静态资源缓存与 API/薪资敏感数据排除；桌面/移动导航、侧栏收起与调宽、移动项目全屏、主要页面响应式、键盘焦点与 reduced-motion 基础 | 设计报告指定的全部 1440×900、1920×1080、390×844 视觉回归和完整无障碍审计仍需持续补齐；离线业务草稿仍只覆盖计时事件队列 |
| 备份与灾难恢复 | 部分实现 | Render PostgreSQL、不可变业务快照、审计和部署健康检查 | 定期导出、R2 与数据库恢复演练、RPO/RTO 记录和自动告警仍未完成 |

## AI 配置与调用核对

- 配置读取和写入都会在服务端查询 `organization_owners`，不能依赖前端隐藏按钮；非唯一 Owner 返回 403。
- API Key 使用独立 `AI_CONFIG_ENCRYPTION_KEY` 加密，浏览器只看到“已配置/未配置”，不会收到密钥原文。
- Worker 使用 OpenAI-compatible `POST {baseUrl}/chat/completions`，发送 Bearer、`model`、`temperature: 0.2`、`max_tokens` 和严格 JSON 消息；输出在入库前再次做结构校验。
- 默认 `glm-4.7-flash` 是当前智谱官方接口列出的有效低成本模型。设计报告提到的 `glm-5.3-flash` 当前不在官方模型枚举中，因此不能为了字面一致而默认调用一个未确认存在的模型。
- Owner 可主动测试当前已保存配置；测试只发送“回复 OK”的极小请求，结果记录供应商域名、模型、HTTP 状态、延迟和脱敏错误，不保存响应正文或密钥。连接测试成功仍不替代 Worker 生成完整报告的端到端验收；供应商错误会明确失败，绝不生成伪报告。

## 发布门槛

每次声称某一行升级为“已实现”前，至少要同时满足：数据库迁移可重复执行、API 服务端权限测试、核心状态机/金额单元测试、桌面和移动关键路径 E2E、空/错/加载状态、Render 构建与健康检查，以及无真实账号、密钥或虚拟业务数据进入 Git。
